import { Inter, Open_Sans } from 'next/font/google';

export const inter = Inter({ subsets: ['cyrillic'], variable: '--font-inter' });

export const lexendDeca = Open_Sans({
  subsets: ['cyrillic'],
  variable: '--font-lexend',
});
